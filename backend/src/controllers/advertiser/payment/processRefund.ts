import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por processar o reembolso de pagamento
export async function processRefundController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

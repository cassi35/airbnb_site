import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por confirmar o pagamento de anúncio
export async function confirmPaymentController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por criar o pagamento de anúncio
export async function createAnnouncementPaymentController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

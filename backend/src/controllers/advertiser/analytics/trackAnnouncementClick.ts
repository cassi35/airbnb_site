import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por registrar clique em anúncio
export async function trackAnnouncementClickController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

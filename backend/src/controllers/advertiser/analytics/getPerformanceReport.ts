import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por retornar relatório de performance dos anúncios
export async function getPerformanceReportController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

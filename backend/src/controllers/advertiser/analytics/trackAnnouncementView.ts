import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por registrar visualização de anúncio
export async function trackAnnouncementViewController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

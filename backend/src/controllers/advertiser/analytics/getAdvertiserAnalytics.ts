import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por retornar dados analíticos do anunciante
export async function getAdvertiserAnalyticsController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por retornar dados analíticos de um anúncio
export async function getAnnouncementAnalyticsController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por deletar um anúncio
export async function deleteAnnouncementController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

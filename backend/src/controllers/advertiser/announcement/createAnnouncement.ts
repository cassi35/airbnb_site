import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por criar um novo anúncio
export async function createAnnouncementController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

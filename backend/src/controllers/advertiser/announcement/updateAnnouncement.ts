import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por atualizar um anúncio
export async function updateAnnouncementController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por renovar o pagamento de anúncio
export async function renewAnnouncementController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

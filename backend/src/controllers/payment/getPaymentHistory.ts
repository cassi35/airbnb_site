import { FastifyRequest, FastifyReply } from "fastify";
// Controller responsável por retornar o histórico de pagamentos do anunciante
export async function getPaymentHistoryController(request:FastifyRequest,reply:FastifyReply):Promise<void>{
}

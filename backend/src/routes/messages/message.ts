import { createMessageController } from "#controllers/mensages/createMessageController.js";
import { defineRoutes } from "#utils/utils.js";

export default defineRoutes(app=>{
    // app.post('/create',createMessageController)
})